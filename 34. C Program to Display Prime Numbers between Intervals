#include <stdio.h>
int main(){
    int a,b,i,j;
    printf("Enter two numbers (range): ");
    scanf("%d %d", &a, &b);
    for(i=a;i<=b;i++) {
        if(i<2) 
        continue;
        for(j=2;j<i;j++){
            if(i%j==0) 
            break;
        }
        if(j==i){
            printf("%d\n", i);
        }
    }
    return 0;
}
