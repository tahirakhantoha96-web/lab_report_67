#include <stdio.h>
int main() {
  int n1,n2,rem,lcm,x,y;
  printf("Enter two  numbers:");
  scanf("%d%d",&n1,&n2);
  x=n1;
  y=n2;
  while(n2!=0){
      rem=n1%n2;
      n1=n2;
      n2=rem;
  }
  printf("GCD:%d\n",n1);
  lcm=(x*y)/n1;
  printf("LCM:%d\n",lcm);

    return 0;
}
