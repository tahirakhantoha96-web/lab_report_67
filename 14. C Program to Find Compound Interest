#include <stdio.h>
int main() {
float principal,rate,time,compoundinterest;
printf("Ener the principal,rate,time:");
scanf("%f%f%f",&principal,&rate,&time);
compoundinterest=(principal*rate*time)/100;
printf("Compound Interest:%.2f\n",compoundinterest);
 return 0;
}
